package com.adel.wc26.core.designsystem.theme

import androidx.compose.ui.unit.dp

/**
 * Spacing scale — a consistent rhythm for padding and gaps across the app.
 * Use these instead of ad-hoc dp values so spacing stays uniform.
 */
object Spacing {
    val xxs = 2.dp
    val xs = 4.dp
    val sm = 8.dp
    val md = 12.dp
    val lg = 16.dp
    val xl = 24.dp
    val xxl = 32.dp
    val xxxl = 48.dp
}